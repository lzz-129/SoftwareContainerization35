apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "film-comments.fullname" . }}-test-connection"
  labels:
    {{- include "film-comments.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "film-comments.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
